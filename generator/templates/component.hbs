import styles from './styles.module.css';

type {{pascalCase name}}Props = {
  title?: string;
};

export const {{pascalCase name}} = ({title,}: {{pascalCase name}}Props) => {
  return (
    <main className={styles['{{camelCase name}}']}>
      <h1>{title}</h1>
    </main>
  );
}
